[project]
name = "analytics"
version = "0.1.0"
description = "Analytics service for URL shortener"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiokafka>=0.12.0",
    "fastapi>=0.111.0",
    "uvicorn>=0.30.1",
]
